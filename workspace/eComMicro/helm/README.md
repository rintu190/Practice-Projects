Helm chart skeletons for services and infra.

Structure:
charts/
  api-gateway/
  user-service/
  product-service/
  order-service/
  analytics-service/

Each chart should be filled in when you're ready to deploy to Kubernetes. Use this directory as a starting point for creating Helm charts that reference container images built from the modules.
